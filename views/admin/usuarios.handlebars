<div class="row">
    {{#each usuarios}}
        <div class="col-sm-4">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">{{nome}}</h5>
                    <p class="card-text">
                        <strong>Email:</strong> {{email}} <br>
                        <strong>Administrador:</strong> {{#if eAdmin}}Sim{{else}}Não{{/if}}
                    </p>
                    {{#unless eAdmin}}
                        <a href="/admin/usuarios/tornarAdmin/{{_id}}" class="btn btn-primary float-right">Tornar Administrador</a>
                    {{/unless}}
                </div>
            </div>
        </div>
    {{/each}}
</div>