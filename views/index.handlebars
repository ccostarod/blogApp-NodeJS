<div class="jumbotron">
    {{#if user}}
        <h2 class="display-4">Bem-vindo ao BlogApp, {{user.nome}}!</h2>
        {{#if user.eAdmin}}
            <a class="btn btn-primary btn-lg" href="/admin" role="button">Gerenciar</a>
        {{/if}}
    {{else}}
        <h1 class="display-4">Bem-vindo ao BlogApp!</h1>
    {{/if}}
    <p class="lead">Olá, esse é um simples blog feito em NodeJS, juntamento com Express e MongoDB, desenvolvido por Rodrigo (admin)</p>
    <hr class="my-4">
    <p>Aqui você encontrará informações sobre o mundo da tecnlogia. Fique por dentro</p>
    {{#unless user}}
        <a class="btn btn-primary btn-lg" href="/usuarios/registro" role="button">Crie sua conta!</a>
    {{/unless}}
</div>
<hr>
<h2>últimas postagens: </h2>

{{#each postagens}}
<div class="card mt-4">
    <div class="card-body">
        <h3>{{titulo}}</h3>
        <p>{{descricao}}</p>
        <a href="/postagem/{{slug}}"><button class="btn btn-success btn-sm">Leia mais</button></a>
        <hr>
        <small>Categoria: {{categoria.nome}}</small><br>
        <small>Data de publicação: {{data}}</small>
    </div>
</div>
{{else}}
    <h5>Nenhuma postagem no momento!</h5>
{{/each}}